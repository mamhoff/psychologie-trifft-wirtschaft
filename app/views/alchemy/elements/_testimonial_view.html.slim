- cache(element) do
  = element_view_for(element) do |el|
    .col-sm-6.col-md-4
      .quote.circle
        blockquote
          .quote-content
            p= el.render :quote
          footer
            cite= el.render :citee
