#fullpage.container
  .row.section#header
    = render_elements(from_page: "header")
  .section#coachings
    = render_cell :coachings
  .section#trainings
    = render_cell :trainings
  .section#orgdev
    = render_cell :orgdev
