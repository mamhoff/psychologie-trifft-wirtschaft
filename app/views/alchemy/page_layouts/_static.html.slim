= render_elements only: [:static_page_header]
= render_elements only: [:static_page]
.row
  = render_elements only: [:testimonial]
= render_elements except: [:static_page, :static_page_header, :testimonial]
